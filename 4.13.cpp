/* nhap 
#include <stdio.h>
#include <math.h>

int main()
{
	float tong = 0;
	int n, x;
	printf("nhap x: ");x, n tinh can(x+can(x+canx(+...+canx))
1. cong thuc
2. for */
	scanf("%d", &x);
	printf("nhap so lan can (n): ");
	scanf("%d", &n);
	for(int i = 1; i <= n; i = i + 1)
	{
		if(x >= 0)
		{
			tong = sqrt(tong + x);
		}
	}
	printf("%f", tong);
}

